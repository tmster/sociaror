<% provide(:title, 'Wszyscy uzytkownicy') %>
<% content_for :menu do %>
<%= render 'menu' %>
<% end %>
<h1>Użytkownicy</h1>

<%= will_paginate %>
              <table class="table table-striped custab">
                    <thead>
                        <tr>
                            <th>ID</th>
                            <th>Nick</th>
                            <th>Email</th>
                            <th>Utworzony</th>
                            <th class="text-center">Akcje</th>
                        </tr>
                    </thead>
                      <% @users.each do |user| %>
               <tr>
                <td><%= user.id %></td>
                <td><%= user.name %></td>
                <td><%= user.email %></td>
                <td><%= user.created_at %></td>
                <td class="text-center">
                <%= link_to '<span class="glyphicon glyphicon-edit"></span>'.html_safe, edit_admin_user_path(user), 
                method: :delete,
                :class=>"btn btn-info btn-xs"  %>
                <%= link_to '<span class="glyphicon glyphicon-remove"></span>'.html_safe, admin_user_path(user), 
                method: :delete,
                :class=>"btn btn-danger btn-xs" ,
                data: { confirm: "You sure?" } %>
                  </td>
               </tr>
            <% end %>


                    </table>


<%= will_paginate %>