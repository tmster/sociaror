<%= bootstrap_form_for(@pytania,style: :horizontal, left: "col-sm-4", right: "col-sm-8") do |f| %>

  <div class="field">
    <%= f.text_field :title %>
  </div>
  <div class="field">
    <%= f.text_area :content, {'data-provide' => "markdown"} %>
  </div>
  <%= f.form_group do %>
    <%= f.submit "Zapisz", class: "btn btn-large btn-primary" %>
  <% end %>
<% end %>