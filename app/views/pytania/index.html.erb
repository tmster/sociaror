<h1>Listing pytania</h1>

<table>
  <tr>
    <th>User</th>
    <th>Parent pytania idtitle</th>
    <th>Content</th>
    <th>Status</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @pytania.each do |pytanium| %>
  <tr>
    <td><%= pytanium.user_id %></td>
    <td><%= pytanium.parent_pytania_idtitle %></td>
    <td><%= pytanium.content %></td>
    <td><%= pytanium.status %></td>
    <td><%= link_to 'Show', pytanium %></td>
    <td><%= link_to 'Edit', edit_pytanium_path(pytanium) %></td>
    <td><%= link_to 'Destroy', pytanium, method: :delete, data: { confirm: 'Are you sure?' } %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Pytanium', new_pytanium_path %>
